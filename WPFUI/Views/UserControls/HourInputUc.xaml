<v:HourInputUcBase
    x:Class="WPFUI.Views.UserControls.HourInputUc"
    xmlns:v="clr-namespace:WPFUI.Views.UserControls"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800"
    x:Name="root">
    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="0,5,0,5">
        <Label Content="{Binding Text, ElementName=root}" Width="NaN" VerticalAlignment="Center" Margin="0,0,10,0" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="60" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Padding="5" VerticalAlignment="Center" x:Name="DecreaseButton" ToolTip="Decrease hour">
                <materialDesign:PackIcon Kind="ChevronDown" Width="16" Height="16" />
            </Button>
            <TextBox Grid.Column="1" x:Name="HourValue" materialDesign:HintAssist.Hint="Hour" 
                     Style="{DynamicResource MaterialDesignOutlinedTextBox}" 
                     HorizontalAlignment="Center" VerticalAlignment="Center" Margin="5,0,5,0" />
            <Button Grid.Column="2" Padding="5" VerticalAlignment="Center" x:Name="IncreaseButton" ToolTip="Increase hour">
                <materialDesign:PackIcon Kind="ChevronUp" Width="16" Height="16" />
            </Button>
        </Grid>
        <Label Content=":" VerticalAlignment="Center" Margin="0,0,10,0" />
    </StackPanel>
</v:HourInputUcBase>
